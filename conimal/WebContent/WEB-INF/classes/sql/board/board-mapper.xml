<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="selectAdoptList">
		SELECT *
		  FROM (
		        SELECT ROWNUM RNUM, A.*
		          FROM (
		                 SELECT 
		                        ADOPT_NO
		                      , M.MEM_ID
		                      , ADOPT_TITLE
		                      , ADOPT_CONTENT
		                      , ADOPT_DATE
		                      , COUNT
		                      , REF_TYPE
		                    FROM ADOPT A
		                    JOIN MEMBER M ON (A.ADOPT_WRITER = M.MEM_NO)
		                   ORDER 
		                      BY ADOPT_DATE DESC
		                ) A
		        )
		 WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<entry key="selectAnimalList">
		SELECT *
		  FROM (
		        SELECT ROWNUM RNUM, A.*
		          FROM (
		                 SELECT 
		                        AN_NO,
		                        AN_TITLE,
		                        AN_PLACE,
		                        AN_DATE,
		                        AN_SPECIES,
		                        AN_GENDER,
		                        AN_WRITEDATE,
		                        AN_CONTENT,
		                        MEM_ID,
		                        REF_TYPE,
		                        AN_COUNT
		                    FROM ANIMAL 
		                    JOIN MEMBER USING (MEM_NO)
		                   ORDER 
		                      BY AN_WRITEDATE DESC
		                ) A
		        )
		 WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<entry key="selectC_PostList">
		SELECT *
		  FROM (
		        SELECT ROWNUM RNUM, A.*
		          FROM (
		                 SELECT 
		                        CPOST_NO,
		                        MEM_ID,
		                        CUSER_NAME,
		                        CPOST_TITLE,
		                        CPOST_COUNT,
		                        CPOST_CONTENT,
		                        CPOST_DATE,
		                        REF_TYPE
		                    FROM C_POST C
		                    JOIN MEMBER M ON (C.CUSER_ID = M.MEM_NO)
		                   ORDER 
		                      BY CPOST_DATE DESC
		                ) A
		        )
		 WHERE RNUM BETWEEN ? AND ?
	
	</entry>
	
	<entry key="selectDonationList">
		SELECT *
		  FROM (
		        SELECT ROWNUM RNUM, A.*
		          FROM (
		                 SELECT 
		                        DO_NO,
		                        DO_TITLE,
		                        DO_CONTENT,
		                        DO_WRITEDATE,
		                        DO_ADMISSION,
		                        MEM_ID,
		                        REF_TYPE,
		                        DO_COUNT
		                    FROM DONATION
		                    JOIN MEMBER USING (MEM_NO)
		                   ORDER 
		                      BY DO_WRITEDATE DESC
		                ) A
		        )
		 WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<entry key="selectVolunteerList">
		SELECT *
		  FROM (
		        SELECT ROWNUM RNUM, A.*
		          FROM (
		                 SELECT 
		                        VOL_NO,
		                        VOL_TITLE,
		                        SPERIOD,
		                        VOL_CONTENT,
		                        VOL_WRITEDATE,
		                        MEM_ID,
		                        REF_TYPE,
		                        VOL_COUNT
		                    FROM VOLUNTEER
		                    JOIN MEMBER USING (MEM_NO)
		                   ORDER 
		                      BY SPERIOD DESC
		                ) A
		        )
		 WHERE RNUM BETWEEN ? AND ?
	</entry>
	
</properties>
